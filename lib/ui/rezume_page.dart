import 'package:flutter/material.dart';

class RezumePage extends StatelessWidget {
  const RezumePage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
appBar: AppBar(title: Text("Rezume"),),
    );
  }
}